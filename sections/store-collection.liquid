

 {% for product in section.settings.product_list-setting %}

   {{ product.images | image_url: width: master | image_tag }}
     {{ product.title }}
    {{ product.price | money }}

{% endfor %}


{% schema %}
  {
    "name": "Store Products",
     "class": "store-product-cover" ,
      "settings": [
      {
          "type": "product_list",
          "id": "product_list-setting",
          "label": "Select Products"
        }
      ],
    
    "presets":[
      {
          "name" : "Store Products"
        }  
    ]
  }
{% endschema %}